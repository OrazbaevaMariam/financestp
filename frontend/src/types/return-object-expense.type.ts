export type ReturnObjectIncomeInputType = {
    error: boolean,
    redirect: null,
    incomes: null,
    response?: null,
}

export type ReturnObjectExpenseType = {
    error: boolean,
    redirect: null,
    expenses: null,
    response?: null,
    
}